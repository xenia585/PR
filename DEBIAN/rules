#!/usr/bin/make -f
# -*- makefile -*-
# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@
	
override_dh_auto_build:
	make -C $(CURDIR)/src all
	
override_dh_auto_install:
	install -D -m 0755 "$(CURDIR)/src/MATRIX" "$(DESTDIR)/usr/local/bin/MATRIX"


